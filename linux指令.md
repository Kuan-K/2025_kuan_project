## 查看空間

```
df -h
```

## 清暫存

```
sudo rm -rf /tmp/*
```

## 清快取

```
sudo apt clean
sudo apt autoremove --purge -y
```

## 資料夾

```
cd [路徑] # 到指定資料夾

cd ..     # 回上一層
cd ../..  # 回兩層

cd ~      # 回到家目錄

cd /      # 回到根目錄

cd -      # 回上一頁(上一個停留的目錄)

pwd  # 顯示你目前所在的完整路徑
ls   # 列出目前資料夾下的所有檔案與子資料夾
```
可使用tab 自動補全
